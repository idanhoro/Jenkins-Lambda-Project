pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'zip -r function.zip .'
            }
        }
        stage('Deploy infrastructure') {
            steps {
                sh 'terraform init'
                sh 'terraform apply -auto-approve'
            }
        }
    }
}