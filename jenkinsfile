pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'tar -czvf function.tar.gz .'
            }
        }
        stage('Deploy infrastructure') {
            steps {
                sh 'cd Terraform/'
                sh 'pwd'
                sh 'echo pwd'
                sh 'terraform init'
                sh 'terraform apply -auto-approve'
            }
        }
    }
}
