pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'tar -czvf function.tar.gz .'
            }
        }
        stage('Deploy infrastructure') {
            steps {
                sh 'pwd'
                sh 'cd Terraform/'
                sh 'pwd'
                sh 'terraform init'
                sh 'terraform apply -auto-approve'
            }
        }
    }
}
